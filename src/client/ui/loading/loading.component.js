import './loading.scss';

/* eslint indent: 0 */
const template = [
  '<div class="loading">',

  '</div>'
].join('');

const component = {
  bindings: {
    loading: '='
  },
  template
};

export default component;
